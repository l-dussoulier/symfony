<!DOCTYPE html>
<html>
    <head>
        <title>Bienvenue</title>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
        <style type="text/css">
            .nav_connect{
              display:inline-block;
            	width:20%;
            	margin-top:10px;
            	text-align:center;
            }
        </style>
    </head>
    <body>

        <center><div class="card" style="width: 45rem; margin-top:8em;" >
          <div class="card-header">
              <h5><center>Contexte du projet</center></h5>
          </div>
          <div class="card-body">
            </br>
            <p>Le CSB se propose d’organiser un système de prêt de matériel informatique d’occasion aux élèves de l’établissement.</br>
               Une application web dédiée est crée pour  l’occasion. </p>

           {% if not app.user %}
             <div class="nav_connect">
                 <a class="btn btn-success" href="{{ path('security_login' )}}">Connexion</a>
             </div>
           {% elseif app.user.role == "ROLE_ADMIN" %}
             <div class="nav_connect">
             <a class="btn btn-primary" href="{{ path('listeUser' )}}">Espace Client</a>
             </div>
             <div class="nav_connect">
                 <a class="btn btn-success" href="{{ path('listeMateriel' )}}">dashboard</a>
             </div>
           {% elseif app.user.role == "ROLE_USER" %}
              <div class="nav_connect">
              <a class="btn btn-primary" href="{{ path('listeUser' )}}">Espace Client</a>
              </div>
           {% endif %}

           {% if  app.user %}
             <div class="nav_connect">
             <a class="btn btn-danger" href="{{ path('security_logout' )}}">Déconnexion</a>
             </div>
           {% endif %}


          </div>
          <div class="card-footer text-muted">
            Par Francisco Santos Et Louis Dussoulier
  </div>
</div></center>
    </body>
</html>
